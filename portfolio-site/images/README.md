# Images

Degree and other assets are downloaded from Google Drive when you run:

```bash
python3 sync_assets.py
```

Or use the full deploy script:

```bash
./deploy.sh
```

Files land here after sync (e.g. `degree.jpg`).
